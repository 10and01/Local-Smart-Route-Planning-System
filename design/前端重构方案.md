# 前端UI重构方案：单文件三视图架构

## 一、项目上下文

**项目**：本地智能路线规划（美团Hackathon）
**技术栈**：FastAPI + Pydantic 后端；Leaflet.js + OpenStreetMap 前端
**数据**：100个杭州真实POI（`data/杭州_pois_mock.json`）+ haversine距离矩阵
**API**：`POST http://localhost:8000/api/plan` → 返回 `PlanResponse`（3条 `RoutePlan`）
**启动**：`cd /d/美团hackthon_5 && source venv/Scripts/activate && python -m backend.main`
**CORS**：`allow_origins=["*"]`

后端引擎已完成调优：
- 4个测试场景相似度控制在 0-40%
- 深度路线4-5个POI，总路程约20km
- 高效路线4-7个POI，总路程4-10km
- 均衡路线6-7个POI

## 二、当前前端诊断

文件：`frontend/index.html`（363行单文件）

**问题**：
1. 侧边栏永远是表单+结果堆叠，空间利用率低
2. 路线详情挤在底部，时间线横向空间不足
3. 地图OSM tiles经常超时，无兜底方案
4. 没有响应式，移动端不可用
5. 无POI详情查看，用户无法细品每个推荐点
6. 三条路线无法直观对比差异
7. 最后一个POI下方错误显示 `transport_to_next`（后端已修复，需前端确认）

**当前API数据字段**（前端可直接使用）：
```javascript
// RoutePlan
{
  plan_id, theme, description, total_time, total_cost, poi_count,
  overall_reasoning, preference_weights_used,
  segments: [{
    poi: {
      poi_id, name, city, district, category, sub_category,
      address, location: {lat, lng},
      tel, rating, price, business_hours, suggested_duration,
      tags, ugc_keywords, highlights, photos, suitable_for
    },
    arrive_time, leave_time, duration,
    transport_to_next, transport_distance_m,
    tips, selection_reasons
  }]
}
```

## 三、目标架构：单文件三视图

不拆分文件，在 `index.html` 内用 CSS+JS 实现三个视图状态：

```
视图0：表单输入（现有，增强过渡动画）
视图1：三方案总览（增强卡片 + 一键对比入口）
视图2：单方案沉浸详情（侧边栏切全屏，垂直时间轴 + POI 详情滑出面板）
视图3：双方案对比（左右分栏，差异高亮）
```

## 四、详细改动清单

### 4.1 HTML结构重构

根容器增加 `data-view` 属性控制当前视图：
```html
<div class="app" id="app" data-view="form">
  <div class="top-bar" id="topBar"></div>
  <div class="main">
    <div class="sidebar" id="sidebar">
      <div class="view view-form" id="viewForm">...</div>
      <div class="view view-overview" id="viewOverview">...</div>
      <div class="view view-detail" id="viewDetail">...</div>
      <div class="view view-compare" id="viewCompare">...</div>
    </div>
    <div class="map-container" id="mapContainer">
      <div id="map"></div>
      <div class="poi-panel" id="poiPanel"></div>
    </div>
  </div>
</div>
```

### 4.2 CSS新增模块

#### 视图系统
- `.app[data-view="form"] .view-form { display: flex; }`
- 其他视图默认 `display: none`，通过 `data-view` 切换
- 视图切换增加 `opacity` + `transform` 过渡动画（300ms）

#### 三方案总览（视图1）
- 卡片左侧增加4px竖条颜色区分：深度=紫色 `#8b5cf6`、高效=绿色 `#10b981`、均衡=蓝色 `#3b82f6`
- 卡片内显示前3个POI名称预览（小字灰色）
- 卡片底部显示总路程（从 `transport_distance_m` 累加，格式化为"约Xkm"）
- 每张卡片右上角增加复选框，勾选两条后底部浮现"进入对比"按钮
- 卡片增加hover阴影和active边框效果

#### 单方案沉浸详情（视图2）
- **头部区域**：
  - 左侧返回箭头 + "返回总览"
  - 中间方案主题（大字）
  - 右侧"对比"按钮
  - 下方标签栏：POI数量、总时间、总花费、总路程、核心标签云（从POI的tags提取高频词）
- **时间轴区域**：
  - 垂直布局，左侧是一条竖线，右侧是POI节点
  - 每个节点：序号圆圈（直径32px）+ POI名称 + 分类标签pill + 到达-离开时间 + 停留时长
  - 节点之间：交通方式图标（🚶/🚌/🚗）+ 距离 + 耗时，用虚线连接
  - 节点可点击，点击后高亮并展开POI详情面板
  - 最后一个节点没有交通信息
- **POI详情滑出面板**（从右侧滑入，宽度320px，覆盖在地图上）：
  - 关闭按钮（X）
  - POI名称（大字）+ 分类标签
  - 评分：用★★★★☆显示（`rating`字段，5分制）
  - 价格：显示"免费"或"¥X"
  - 标签列表：每个tag做成小pill，带颜色（美食=橙、拍照=粉、文化=紫、自然=绿、购物=蓝）
  - 选择理由：`selection_reasons[0]`，用浅紫色背景高亮
  - Tips：如果有`tips`，用浅绿色背景显示
  - UGC关键词：`ugc_keywords`数组，做成tag cloud样式
  - 营业时间：`business_hours`
  - 地址：`address`
  - 照片区域：由于mock数据无真实照片，用Unsplash搜索URL占位，格式如 `https://source.unsplash.com/400x300/?杭州,西湖`（或关键词从POI tags取第一个）

#### 双方案对比（视图3）
- 顶部："返回总览" + 对比标题"A vs B"
- 中间：对比摘要栏
  - 用表格形式展示：POI数量、总时间、总花费、总路程
  - 差异用箭头表示：↑多/↓少
- 主体：左右分栏，每栏一个方案的完整时间轴
  - 相同POI（按 `poi.name` 匹配）的节点背景高亮浅绿色
  - 不同POI正常显示
- 底部："仅A有"和"仅B有"的POI列表

#### 地图增强
- **Tiles兜底**：
  - 先加载OSM：`https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png`
  - 设置3秒超时检测，如果未加载成功，自动切换到 CartoDB Positron：`https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png`
  - 如果仍失败，显示灰色底图 + "地图加载中..."文字
- **Marker美化**：
  - 使用 `L.divIcon` 创建自定义Marker
  - 样式：白色圆形背景，紫色边框，内部显示序号（1/2/3...）
  - 当前高亮的POI Marker放大1.2倍
- **Polyline增强**：
  - 实线替代虚线（`dashArray`去掉）
  - 在线段中间添加方向箭头（可用Leaflet.PolylineDecorator或手动在polyline上叠加小三角形Marker）
  - 颜色按方案区分：深度=紫色、高效=绿色、均衡=蓝色
- **无tiles兜底**：Leaflet tileerror事件监听，失败后设置容器背景色为 `#e5e7eb`，中央显示 "地图数据加载中" 文字

#### 加载与错误状态
- 按钮loading：spinner旋转 + 文字"正在分析你的偏好..."
- 5秒后未返回：文字变为"正在深度计算最佳路线..."
- API失败：表单下方显示红色错误横幅（`background: #fee2e2; color: #991b1b; padding: 10px; border-radius: 6px;`）
- 后端未启动：`请求失败，请确保后端服务已启动：python -m backend.main`

#### 响应式适配（@media max-width: 768px）
- `.sidebar` 宽度变为 `100vw`
- `.map-container` 默认隐藏，增加底部tab切换（"列表" / "地图"）
- 表单字段全部单列
- 方案卡片单列全宽
- 单方案详情的时间轴正常显示，POI详情面板改为底部弹出（`position: fixed; bottom: 0; left: 0; right: 0; max-height: 60vh;`）
- 双方案对比改为上下堆叠（A在上，B在下）
- 顶部导航栏显示当前视图标题 + 返回按钮

### 4.3 JS逻辑重构

#### 状态管理
```javascript
let appState = {
  view: 'form',           // form | overview | detail | compare
  plans: [],              // 三条路线
  activePlanIndex: 0,     // 当前查看的方案
  selectedForCompare: [], // 选中的对比方案索引 [0, 2]
  activePoiIndex: null,   // 当前高亮的POI索引（用于详情面板）
  map: null,
  routeLayer: null,
  markers: []
};
```

#### 视图切换函数
```javascript
function switchView(viewName, options = {}) {
  appState.view = viewName;
  document.getElementById('app').dataset.view = viewName;
  updateTopBar(viewName);
  if (viewName === 'overview') renderOverview();
  if (viewName === 'detail') renderDetail(options.planIndex);
  if (viewName === 'compare') renderCompare();
}
```

#### 渲染函数清单
- `renderOverview()`：渲染三方案卡片列表
- `renderDetail(planIndex)`：渲染单方案详情时间轴
- `renderCompare()`：渲染双方案对比
- `renderPoiPanel(planIndex, poiIndex)`：渲染POI详情面板
- `showPlanOnMap(planIndex)`：在地图上显示指定方案的POI和连线
- `highlightPoiOnMap(planIndex, poiIndex)`：高亮指定POI的Marker

#### 事件绑定
- 表单提交 → `generatePlan()` → 成功后 `switchView('overview')`
- 点击卡片 → `switchView('detail', {planIndex: idx})`
- 点击时间轴节点 → `renderPoiPanel(planIndex, poiIndex)` + `highlightPoiOnMap(...)`
- 点击"返回" → 根据当前视图决定返回到哪里
- 复选框勾选 → 更新 `selectedForCompare`，满2个时显示"进入对比"按钮

### 4.4 关键交互细节

1. **从表单到总览的过渡**：表单区域向上滑出，总览区域淡入，地图保持
2. **从总览到详情的过渡**：总览向左滑出，详情从右侧滑入，地图重新定位到该方案
3. **POI面板行为**：
   - 点击时间轴节点 → 面板从右侧滑入（桌面端）/ 从底部滑入（移动端）
   - 点击地图Marker → 同样触发面板
   - 点击面板外部或X按钮 → 面板关闭
   - 面板打开时，对应Marker放大并跳动（`L.marker(...).setZIndexOffset(1000)`）
4. **地图与侧边栏联动**：
   - 侧边栏滚动时间轴时，地图中心跟随当前可见的POI
   - 地图点击Marker时，侧边栏自动滚动到对应节点

## 五、后端微调（均衡策略）

修改文件：`backend/core/route_engine.py`

在 `compute_poi_marginal_value` 函数的 `balanced` 分支中增加地理连续性奖励：

```python
elif strategy == "balanced":
    if dist_km > 8 and pref_match > 0.4:
        marginal_value += (dist_km - 8) * pref_match * 5
    # 新增：地理连续性奖励
    if dist_km <= 3:
        marginal_value += 8
    elif dist_km > 6:
        marginal_value -= (dist_km - 6) * 5
```

**不要修改**：
- `experience` 策略的任何参数
- `efficiency` 策略的任何参数
- `force_differentiate` 的逻辑
- `preference_guided_greedy` 的主循环结构

## 六、验收标准

| # | 验收项 | 验证方式 |
|---|--------|---------|
| 1 | 单方案详情页可正常进入和返回 | 点击卡片→显示时间轴→点击返回→回到总览 |
| 2 | 时间轴每个POI可点击展开详情面板 | 点击节点→右侧面板滑出→显示评分/标签/理由/UGC关键词 |
| 3 | 地图tiles超时3秒后自动切换备用源 | 断网模拟或DevTools限速→3秒后地图仍有底图 |
| 4 | Marker带序号圆圈，polyline有方向感 | 肉眼检查Marker样式和线条 |
| 5 | 可勾选两条路线进入对比模式 | 勾选两张卡片→显示"进入对比"→点击后左右分栏显示 |
| 6 | 对比模式相同POI高亮 | 左右两侧相同POI名称背景变绿 |
| 7 | 响应式：iPhone SE模拟器下可正常操作 | Chrome DevTools切iPhone SE→表单/卡片/时间轴均正常显示 |
| 8 | API失败不弹alert，显示红色横幅 | 停掉后端→点击生成→看到红色错误条 |
| 9 | 均衡路线总路程平均降低15%+ | 运行4个测试场景→均衡路线总路程对比之前数据 |
| 10 | 三条路线相似度仍<45% | 运行4个测试场景→所有相似度对<45% |


